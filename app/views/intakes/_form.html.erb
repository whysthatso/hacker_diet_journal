<%= form_with(model: intake) do |form| %>
  <% if intake.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(intake.errors.count, "error") %> prohibited this intake from being saved:</h2>

      <ul>
        <% intake.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :ean, style: "display: block" %>
    <%= form.number_field :ean %>
  </div>

  <div>
    <%= form.label :kcal_per_100, style: "display: block" %>
    <%= form.number_field :kcal_per_100 %>
  </div>

  <div>
    <%= form.submit class: [ :button, :'is-primary' ] %>
  </div>
<% end %>
